spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/spring_library #?characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: root
    password: wngks0910
    driver-class-name: com.mysql.cj.jdbc.Driver
    show-sql: true
    defer-datasource-initialization: true